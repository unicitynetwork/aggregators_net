name: Build Aggregator client Docker image

on:
  push:
    branches:
      - main
      - devnet

jobs:
  build:
    outputs:
      image_name: ${{ steps.extract_name.outputs.image_name }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: build_and_push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}

      - name: Extract image name
        id: extract_name
        run: |
          image_name=$( echo "${{ steps.build_and_push.outputs.metadata }}" | jq -r .image.name )
          echo "image_name=${image_name}"
          echo "image_name=${image_name}" >> $GITHUB_OUTPUT

# call-deploy:
#   needs: build
#   uses: ./.github/workflows/deploy.yml
#   with:
#     aggregator_tag: ${{ needs.build.outputs.image_name }}
#     environment: devnet
